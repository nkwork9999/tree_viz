import React from "react";

export default function opencv() {
//   async function takepicture() {
//     const cotext = canvas.getContext("2d");
//     if (width && height) {
//       canvas.width = width;
//       canvas.height = height;
//       context.drawImage(video, 0, 0, witdh, height);

//       const data = canvas.toDataURL("image/png");
//       const b64 = data.replace(/:,/, "");
//       const b64_mod = await invoke("image_receive", { name: "test", img: b64 });

//       const mod_data = "data:image/png;base64," + b64_mod;
//       PushSubscriptionOptions.setAttribute("src", mod_data);
//     } else {
//       clearphoto();
//     }
  }
  return <div>opencv</div>;
}
